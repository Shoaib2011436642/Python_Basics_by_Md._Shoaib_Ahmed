import os                   #import os library to perform deletation

#create a file, then remove it.
#createfile = open("myfile.text", "w")
#createfile.write("Real Madrid")

#os.remove("myfile.text")            #to delete use remove function along with mentioning the library

#os.rmdir("New File")                #removes or deletes the existing file

# TO perform each deletation, remove the hash(#)