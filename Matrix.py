matrix_list = [
    [1,2,3,4],
    [5,6,7,8],
    10
]

print(matrix_list)                  #prints the matrix or nested loop (list inside a list)

print(matrix_list[0])               #prints a specific value of the matrix (here the value is a list)

print(matrix_list[0][2])            #prints a specific value of a list value of the matrix,
                                    #Here 1st 3rd bracket indicates the index of the value list of the matrix
                                    #The 2nd 3rd bracket indicates the index of the specific value of that specific list of the matrix