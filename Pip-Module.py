# pip - package manager (brings the package or module)
        ''' We call a module through pip
                        Then we use the module
                                                '''

# module - package (ready to use, file consisting of py codes) - made by py developers

# 2 types of module - Built in module       |       User-defined or External module - We call it through pip - mostly used in ML DL AI