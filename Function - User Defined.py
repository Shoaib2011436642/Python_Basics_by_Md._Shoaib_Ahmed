def func_sum(a, b):             #need to put parameters in the paranthesis that will be passed as argument to store values in it.
    sum = a + b
    print(sum)

func_sum(15, 20)        #storing the values in the passed argument (parameter variables).
func_sum(10, 40)